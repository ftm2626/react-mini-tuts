// import { QueryClient, QueryClientProvider } from "@tanstack/react-query";

export default function Home() {
  // const queryClient = new QueryClient();
  return (
    // <QueryClientProvider client={queryClient}> // this is the old way of setting up react-query in next, but for next 13 we need a custome provider
    <main></main>
    // </QueryClientProvider>
  );
}
